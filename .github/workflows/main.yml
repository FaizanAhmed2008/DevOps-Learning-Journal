name: My-First-Github-Action

on: 
  push: 
    branches: [ main ]

jobs:
  First-Job:
    runs-on: ubuntu-latest

    steps:
      - name: First-Action
      run: echo " Yo this is my first action ? is it working ? "
