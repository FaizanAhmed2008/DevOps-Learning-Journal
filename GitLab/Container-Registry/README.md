# GitLab Container Registry

This section covers GitLab's built-in container registry:

- Using the GitLab Container Registry for Docker images.
- Authenticating and pushing images.
- Tagging and managing image versions.
- Deploying container images from GitLab.
