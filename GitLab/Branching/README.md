# GitLab Branching

This section explains branching strategies used with GitLab projects:

- Feature branches and topic branches.
- GitLab Flow and merge request workflows.
- Branch naming conventions.
- Maintaining long-lived branches like `main`, `develop`, and `release`.
- Handling branch cleanup and merge workflows.
