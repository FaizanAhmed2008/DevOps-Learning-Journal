# GitLab Basic Commands

I wrote this note while I was learning GitLab repository setup and the commands I use every day.

This folder is my command reference for the first GitLab workflows I practiced.

## What I learned here

- Cloning GitLab repositories and configuring remotes.
- Using `git status`, `git diff`, and `git log` to inspect work.
- Creating branches, committing changes, and pushing to GitLab.
- Syncing feature branches with `main` and keeping work clean.
- Preparing commits for merge requests.

## Command sheet

See `cmds.md` for the exact commands I use during practice.
