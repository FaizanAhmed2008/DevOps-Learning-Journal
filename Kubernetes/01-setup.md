# Kubernetes Setup

## Commands
- Check installed tools:
  - `kubectl version --client`
  - `minikube version`
- Start local cluster:
  - `minikube start`
- Check cluster status:
  - `kubectl get nodes`
