# Docker Images

- An image is a read-only template used to create containers.
- Pull an image with `docker pull <image>`.
- List images with `docker images`.
- Remove an image with `docker rmi <image>`.
