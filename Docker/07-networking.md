# Docker Networking

- Containers can communicate through Docker networks.
- List networks with `docker network ls`.
- Create a network with `docker network create <name>`.
- Connect containers using `--network <name>`.
