# Container Registry

- A registry stores and shares Docker images.
- Docker Hub is the default public registry.
- Push an image with `docker push <image>`.
- Pull from a registry with `docker pull <image>`.
