# Git Ignore Commands

- Create a .gitignore file: `touch .gitignore`
- Example ignore patterns:
  - `node_modules/`
  - `.env`
  - `*.log`
  - `dist/`
- Check ignored files with Git status: `git status`