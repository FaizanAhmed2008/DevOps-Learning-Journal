# Git Merging Commands

- Create a new branch: `git switch -c feature-auth`
- Switch to the main branch: `git switch main`
- Merge a feature branch into main: `git merge feature-auth`
- View the commit graph: `git log --oneline --graph --all`
